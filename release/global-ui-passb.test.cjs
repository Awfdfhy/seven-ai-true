"use strict";
const assert=require("assert/strict"),core=require("./global-ui-contract.cjs"),p=require("./global-ui-passb.cjs");let n=0;const ok=(v,m)=>{assert.ok(v,m);n++},eq=(a,b)=>{assert.equal(a,b);n++},throws=(f,re)=>{assert.throws(f,re);n++};
const audit={schema:"seven.global-ui-audit.v1",version:core.VERSION,status:"PASS",fail:[],warn:[],info:[]};audit.auditHash=core.hash(audit);const manifest=core.createManifest({branch:"ultimate-polish-v1",commitSha:"a".repeat(40),sources:[{path:"release/x.css",content:"x"}],audit});const receipt=core.createFoundationReceipt({manifest,startupBytes:99000,protectedBlob:"b".repeat(40),visual:{scenarios:8,pass:8,warn:0,fail:0,tier:"HOST",mode:"OBSERVE"}});
const z=p.createZeroManualReport({naturalComposer:true,workspaceDiscovery:true,stateLegibility:true,advancedControlsProgressive:true,recoveryReachable:true});ok(p.verifyZeroManualReport(z));eq(z.status,"PASS");const zBad=p.createZeroManualReport({naturalComposer:true});eq(zBad.status,"FAIL");ok(zBad.missing.includes("workspaceDiscovery"));
const host=p.createClaim({foundationReceipt:receipt,kind:"HOST_FOUNDATION",evidenceTier:"HOST"});ok(p.verifyClaim(host,receipt));eq(host.evidenceTier,"HOST");throws(()=>p.createClaim({foundationReceipt:receipt,kind:"HOST_FOUNDATION",evidenceTier:"RELEASE_BUILD_DEVICE"}),/relabeled/);throws(()=>p.createClaim({foundationReceipt:receipt,kind:"ANDROID_VISUAL_CERTIFICATION",evidenceTier:"HOST",deviceProof:"c".repeat(64)}),/release-build-device/);throws(()=>p.createClaim({foundationReceipt:receipt,kind:"ANDROID_VISUAL_CERTIFICATION",evidenceTier:"RELEASE_BUILD_DEVICE",deviceProof:"bad"}),/deviceProof/);const device=p.createClaim({foundationReceipt:receipt,kind:"ANDROID_VISUAL_CERTIFICATION",evidenceTier:"RELEASE_BUILD_DEVICE",deviceProof:"c".repeat(64)});ok(p.verifyClaim(device,receipt));
const checks=["day-night","mobile-overflow","touch-targets","focus-halo","reduced-motion","lite-tier","protected-source"];const gate=p.createPassBGate({foundationReceipt:receipt,zeroManual:z,claim:host,checks});ok(p.verifyPassBGate(gate));eq(gate.boundary,"NO_ANDROID_CERTIFICATION_CLAIM");throws(()=>p.createPassBGate({foundationReceipt:receipt,zeroManual:z,claim:host,checks:checks.slice(1)}),/missing PassB check/);throws(()=>p.createPassBGate({foundationReceipt:receipt,zeroManual:zBad,claim:host,checks}),/zero-manual/);throws(()=>p.createPassBGate({foundationReceipt:receipt,zeroManual:z,claim:device,checks}),/host foundation claim/);console.log(`Global UI Pass B: PASS (${n} assertions)`);
