# Seven Shell v3

Internal integration marker for the final shell pass. The runtime remains split into lazy local CSS/JS so Android startup stays light. This pass unifies navigation, composer controls, workspace access, settings surfaces, message actions, responsive keyboard behavior, and visible release branding without changing the protected source file.
