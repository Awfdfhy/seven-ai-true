# Seven AI — Ultimate Polish Plan

> **Purpose:** Authoritative refinement campaign for the complete capability inventory in `CAPABILITY_REGISTRY.md`. This plan defines how systems are audited, challenged, accelerated, evaluated and frozen. It is not a blanket implementation claim.

## Prime Directive
For every canonical Seven capability and every Tool Fabric 2.0 capability, ask:

> What is the strongest practical version we can build while preserving truth, safety, recoverability, Android efficiency, maintainability and a coherent Seven experience?

Polish strengthens systems. It does not reward random feature accumulation.

## Campaign Upgrade — Capability-Level Polish
The earlier 12 Boss Systems remain useful grouping containers, but they are no longer the smallest polish unit. **Every capability in `CAPABILITY_REGISTRY.md` receives its own dedicated campaign.** Tool Fabric 2.0 Waves 01–19 and their resulting contracts/brokers/capabilities are included.

For each capability:
1. reconstruct current architecture and implementation truth;
2. identify dependencies and invariants;
3. run Maximum Effort Polish Pass A;
4. challenge the result adversarially;
5. run Maximum Effort Polish Pass B independently against the strengthened state;
6. allow `NO_MATERIAL_IMPROVEMENT` if evidence supports it;
7. apply Speeding & Smoothness analysis;
8. define implementation/eval plan and freeze criteria;
9. preserve results in GitHub Project Memory.

Two passes mean two opportunities to discover material improvement, not two mandatory rewrites. Never add complexity merely to make Pass B look productive.

## The 12 Boss Systems
1. Brain / Cognitive Runtime
2. Models / Routing / Adaptive Compute
3. Memory
4. Context
5. Tools / Permissions / Side Effects
6. Coding Agent
7. Research
8. RPG / Real Works
9. Verification / Evals / Self-Evolution
10. UX / Brand / Motion / Accessibility
11. Performance / Local Intelligence / Android Constraints
12. Recovery / Integrity / Security / Observability

Each Boss System aggregates capability-level evidence rather than replacing it.

## Required Capability Audit
Every capability receives, where applicable: architecture and implementation truth; user value and failure cost; dependencies/interfaces/invariants; bottlenecks; correctness/epistemic/security risks; recovery/idempotency/cancellation behavior; Android resource cost; UX/RTL/accessibility impact; competing designs; adversarial challenge; before/after evaluation; simplification opportunities; and freeze/saturation criteria.

## Seven Speeding & Smoothness System ⚡
This is a cross-cutting optimization layer applied to **every** capability rather than a final one-time performance pass.

Optimize cold/warm startup, time-to-interactive, time-to-first-useful-result, end-to-end and tail latency, frame stability, input/cancellation responsiveness, scheduling/concurrency, redundant model/tool/retrieval/verification work, context compilation, memory pressure, cache correctness, progressive/lazy loading, network/storage cost, background work, thermal/battery pressure and truthful perceived responsiveness.

### Speed Laws
- remove waste before removing capability;
- use cheap/direct paths before expensive escalation when valid;
- cache only with correct authority/freshness/identity boundaries;
- parallelize only independent work;
- cancel obsolete work aggressively when safe;
- prioritize critical-path work;
- defer optional heavy work;
- performance tiers may reduce decoration/optional depth but not required correctness or safety;
- measure tail latency and jank, not only averages.

## Maximum Effort Execution
Every substantial pass uses `MAXIMUM_EFFORT_PROTOCOL.md`. The command is generated for the exact capability and explicitly includes research, architecture, adversarial failure search, implementation reality, mobile cost, speed/smoothness, verification, simplification and preservation.

The protocol itself can be polished when a material weakness is discovered. It is not a magic immutable prompt.

## Tool Fabric 2.0 Integration
Tool Fabric 2.0 is part of this campaign, including Waves 01–19, canonical tool contracts, SchemaGuard, MCP/interoperability, SearchBroker and related broker/discovery/validation infrastructure. Each tool capability is judged by coverage, reliability, authority/permissions, side effects, schema/result validation, provenance, cancellation, idempotency, latency, resource cost and integration quality, not by tool count.

## Visual Intelligence & Design Fabric V1.4
Visual architecture reached `ARCHITECTURE_SATURATED_2_OF_2` after two consecutive challenge rounds returned `NO_MATERIAL_IMPROVEMENT`.

The visual system covers UI, imagery, logo/icon, raster/vector, motion, Arabic/RTL, accessibility, Android adaptive/themed icons, typed/generated UI, visual/human judging, performance/privacy, anti-copy/distinctiveness, Design Lint, regression evidence and post-release visual health.

### Visual Implementation Mega-Waves
1. Visual Evidence Runtime
2. Design Genome Runtime
3. Logo & Identity Tournament
4. Global UI
5. Specialist workspaces: Coding / Research / RPG-Real Works
6. Typed / Generated UI
7. Motion
8. Arabic / RTL
9. Accessibility
10. Mobile Performance
11. Android Visual Certification
12. Visual Red Team + implementation-level saturation

Architecture saturation does not waive these implementation gates.

## Design Genome Rule
Seven must develop recognizable product DNA rather than copying another AI application or design system. Candidate primitives such as Orbit Thread, Seven Cut, Evidence Rail, Focus Halo and State Node are hypotheses until tested. Token governance and Design Lint prevent later UI from drifting outside the selected genome.

## Logo Tournament Rule
The final mark must survive small-size legibility, monochrome, Day/Night, adaptive masks, themed icons, splash/motion, distinctiveness and implementation/resource cost. Earlier logo concepts remain candidates/history until a winner earns freeze.

## RPG / Real Works Polish
Polish prioritizes canon provenance, source hierarchy, timeline/event integrity, character/world/knowledge state, rules/invariants, insertion constraints, explicit divergence, `CANON_GAP`, scene validation and world-specific linguistic/title DNA. Prose quality cannot compensate for state/canon corruption.

## Freeze / Saturation Rule
A capability freezes only when its relevant architecture, behavior, failure modes, performance, security/integrity, verification and UX gates are satisfied. Two consecutive valid challenge rounds with no material improvement may support saturation for the scope being tested. A material change to the constitution, implementation or evidence environment can reset that saturation.

## Campaign Order
Use dependency-aware ordering rather than blindly walking a numbered list. Current immediate priority is the 12-Wave visual implementation campaign. Across the broader project, polish upstream foundations before dependent surfaces when doing so prevents rework.

## Completion Truth
Ultimate Polish is complete only when capability-level records, implementation truth and evaluation evidence support completion. A strong plan is not implementation; an implemented feature is not verified release quality; and a visually impressive result is not proof of correctness.