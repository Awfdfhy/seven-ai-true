const assert=require('assert');
const {SevenUltimateOS}=require('./src/ultimate/runtime.js');
const {RPGDirector}=require('./src/ultimate/rpg-director.js');

(()=>{
 const os=new SevenUltimateOS();os.createRpg({gameId:'g',campaignId:'c',timelineId:'t',name:'Valen'});os.core.registerEntity('g',{id:'ali',name:'Ali',tier:'core'});os.core.registerEntity('g',{id:'aria',name:'Aria',tier:'core'});os.characters.createProfile('g','ali',{values:['freedom'],voice:{humor:'high'}});os.characters.createProfile('g','aria',{values:['autonomy','duty'],philosophy:['Choice must survive power'],voice:{formality:'high'}});os.core.createTrack('g','c','t',{id:'side',kind:'independent_arc'});os.parallelStories.bindContract('g','c','t','side',{mainlineImpact:'minimal',permanentWorldDestruction:false});
 const e=os.commitBeat({id:'old',actorEntityId:'aria',playerEntityId:'ali',actionType:'dialogue',payload:{text:'Old promise about the crown'},rememberBy:['ali','aria']});
 const director=new RPGDirector({core:os.core,memory:os.memory,story:os.story,narrative:os.narrative,tone:os.tone,flashbacks:os.flashbacks,characters:os.characters,parallelStories:os.parallelStories});director.startScene({id:'scene1',gameId:'g',campaignId:'c',timelineId:'t',playerEntityId:'ali',trackId:'side',episodeId:'ep-side-1',location:'Palace',time:'Night'});
 const bundle=director.prepare({sceneId:'scene1',userInput:'crown promise',activeCharacters:['ali','aria'],episodeNumber:3,conflict:{type:'political'},flashbackCandidate:{kind:'canonical_recall',sourceEventRefs:[e.ref],contextRelevance:100,characterRelevance:100,causalImportance:90,emotionalResonance:80,plotPayoff:80,unansweredQuestionValue:70}});assert.equal(bundle.authority,'derived_plan');assert.ok(bundle.minds.ali.memories.some(x=>x.event.id==='old'));assert.equal(bundle.sideContract.mainlineImpact,'minimal');assert.ok(bundle.flashback&&bundle.flashback.score.recommended);
 const safe=director.proposeBeat('scene1',{actorEntityId:'aria',actionType:'dialogue',agencySignals:{directQuestion:false}});assert.equal(safe.state,'speculative');director.commitSafeBeats('scene1');const gate=director.proposeBeat('scene1',{actorEntityId:'ali',actionType:'opinion',agencySignals:{opinionRequested:true}});assert.equal(gate.state,'gate');assert.equal(gate.ui.type,'agency_gate');const parsed=director.playerInput('scene1','"No." [Think: This is suspicious.]');assert.equal(parsed.say[0],'No.');assert.equal(parsed.think[0],'This is suspicious.');
 const impact=director.validateSideImpact('scene1',{permanentWorldDestruction:true,mainlineImpact:'major'});assert.equal(impact.pass,false);assert.equal(impact.promotionSuggested,true);const bad=director.validateNarration({playerEntityId:'ali',actorEntityId:'ali',actionType:'decision',userAuthored:false,text:'Ali decided for himself.'});assert.equal(bad.pass,false);assert.ok(bad.issues.includes('PLAYER_AGENCY_VIOLATION'));
 const fb=director.enterFlashback('scene1',{kind:'canonical_recall',contextRelevance:100,characterRelevance:100,causalImportance:100,emotionalResonance:100,plotPayoff:100,unansweredQuestionValue:100});assert.equal(fb.mode,'flashback');assert.equal(fb.trackId,'side','flashbacks must work inside independent/side tracks');assert.equal(director.returnFromFlashback('scene1').mode,'present');const done=director.finish('scene1');assert.equal(done.status,'complete');
 console.log('ultimate RPG director: 18 assertions PASS');
})();